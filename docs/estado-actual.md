# Estado actual

Registro cronológico de cambios significativos, bugs corregidos y pendientes. Cada entrada describe el estado que dejó esa sesión de trabajo.

---

- **API completa**: todos los endpoints documentados están implementados. Incluye `/reports/months` y `?month=YYYY-MM` en summary / by-category / net-worth para el selector de mes del dashboard.
- **Dashboard implementado**: todas las cards activas. Selector de mes (GET /reports/months) sincroniza PatrimonioCard, StatRow, HealthCard y CategoryChart. RunwayCard y NetFlowChart son independientes del mes seleccionado. RecentActivity filtra por el mes activo. Backend extiende /reports/net-worth y /reports/summary con datos del mes anterior (previousTotal/changePercent y previousExpense/expenseChangePercent) para los badges de variación — sin doble llamada.
- **Frontend funcional end-to-end**: auth, funds, transactions, transfers, categories, health, dashboard.
- **Settings implementado**: pantalla `/ajustes` completa — perfil (displayName editable, cambiar contraseña con logout automático), framework por defecto, runway fund toggles, tema claro/oscuro, exportar .xlsx. Botones Importar y Reconfigurar fondos habilitados, navegan a `/import` y `/onboarding?from=settings` respectivamente. Se agregó `refetchUser()` al `AuthContext` para que el sidebar refleje el nombre actualizado sin recargar.
- **Health — cambio de modelo**: el campo `config` jsonb fue eliminado de `HealthProfile`. Los targets del framework viven como `frameworkSlot` + `targetPercentage` en cada `Fund`, sembrados automáticamente al cambiar de framework. El patrón Strategy fue evaluado y eliminado; la lógica es inline en `health.service.ts`.
- **Health — recomendaciones (pendiente IA)**: la sección "Recomendaciones" de la pantalla `salud` está temporalmente oculta. La lógica estática (`getRecommendations`, `RecommendationCards`, `RECS` en `utils.ts`) existe pero no se renderiza. La intención es reemplazarla con recomendaciones personalizadas generadas por un LLM a partir del comportamiento financiero real del usuario (assessment + historial de transacciones). No eliminar los archivos existentes — son la base de la futura integración.
- **Import/Export**: pantalla `/import` implementada. Wizard 3 pasos: upload → preview (stats + tabla de filas + sección fondos desconocidos colapsable con checkboxes) → éxito. El usuario controla qué fondos desconocidos aprobar; filas de fondos no aprobados se excluyen del commit. Export .xlsx sigue en Ajustes.
- **Transacciones — bugs corregidos**: (1) lista no se refrescaba tras editar/eliminar — mutations no invalidaban `['activity']`; corregido agregando invalidación a los tres hooks. (2) fecha no se pre-poblaba al editar — `occurredOn` venía como ISO completo (`T04:00:00.000Z`) y se pasaba directo al `<input type="date">`; corregido con `.slice(0, 10)`. (3) filas reordenaban al editar categoría — consecuencia del bug de fecha anterior.
- **Transacciones — filtros persistentes por URL**: `tab`, `from`, `to`, `fundId`, `categoryId`, `q` y `page` viven en search params (`useSearchParams`). El sidebar restaura los últimos params guardados en `sessionStorage['tx:params']` al navegar a `/transacciones`. Botón Atrás del browser también restaura el estado.
- **Transacciones — DateInput consistente**: los inputs de fecha del panel de filtros usan el mismo componente `DateInput` custom (calendario popup en español) que el formulario de edición. Se agregó prop `placeholder` opcional al componente (default: `'Selecciona una fecha'`); el filtro usa `'Desde'` / `'Hasta'`. Se corrigió desbordamiento del calendario cuando el trigger está cerca del borde derecho del viewport — ahora se alinea al borde derecho del trigger si no cabe a la izquierda.
- **Transacciones — exportar**: botón Exportar habilitado en la toolbar. Llama `GET /export?from=&to=` con los filtros de fecha activos y descarga el `.xlsx`. Sin fechas filtradas exporta todo. El nombre del archivo incluye el rango si hay filtro activo (`wealet-export-YYYY-MM-DD-YYYY-MM-DD.xlsx`).
- **Dashboard — RecentActivity filtra por mes**: el componente `RecentActivity` acepta el mes activo y pasa `from`/`to` al endpoint `/activity`. Antes era independiente del selector de mes.
- **Dashboard — HealthCard top-3 para Fondos**: cuando el framework activo es `fondos` y el usuario tiene más de 3 fondos, el card muestra los 3 con mayor saldo (`actualAmount` DESC) y un pie con "+N fondos más / Ver todos" que lleva a `/salud`. Para `50/30/20` y `jars_eker` sigue mostrando todos los slots.
- **Dashboard — RunwayCard con gestión de fondos colchón**: el `RunwayCard` muestra un link "Ver fondos colchón (N)" al pie que abre un drawer lateral (`RunwayFundsDrawer`). El drawer lista todos los fondos activos — primero los que tienen `countsForRunway: true` — con toggle por fila para activar/desactivar su participación en el runway. El footer del drawer muestra el colchón total en tiempo real. Cambios se guardan vía `PATCH /funds/:id` inmediatamente; invalida `['funds']` y `['reports', 'runway']` para mantener el card sincronizado. Sin cambios en la API.
- **Dashboard — CategoryChart con drawer de desglose completo**: el `CategoryChart` muestra el top 6 de categorías por gasto. Cuando hay más de 6, aparece "+N categorías más / Ver todas" al pie. "Ver todas" abre `CategoryChartDrawer` — drawer de solo lectura con todas las categorías del mes ordenadas por monto, mismas barras proporcionales, y footer con el gasto total del mes en BigInt. Sin fetch adicional (datos ya en cache de `useByCategory`).
- **Transacciones — ConfirmDialog en eliminar**: el botón de eliminar en `TransactionFormModal` usaba `window.confirm` nativo. Reemplazado por el componente `ConfirmDialog` estilizado, consistente con el patrón ya usado en `FundFormDrawer`.
- **Onboarding implementado**: wizard full-screen de 3 pasos — selección de preset (jars_eker, 50/30/20, profit_first, fondos propios, Excel), preview de fondos, pantalla de éxito. `ProtectedRoute` redirige a `/onboarding` si `onboardingCompleted = false`. El preset `sobres` fue renombrado a `profit_first` (Profit First, basado en Mike Michalowicz) con fondos Estilo de Vida / Diversión / Inversión / Seguridad, slots predefinidos y framework de salud propio. jars_eker nombres sincronizados entre `fund-presets.ts` y `framework-funds.ts`. El camino Excel embebe `UploadStep` + `PreviewStep` del feature de import sin redirigir fuera del wizard.
- **POST /funds/preset**: acepta `profit_first` como valor de preset. El valor `sobres` fue eliminado del enum.
- **Monthly Allocation implementado**: POST/GET `/monthly-allocation/current` crea distribución del ingreso mensual como N transacciones de ingreso (una por fondo activo).
- `getFondosAssessment` eliminado — todos los frameworks (incluido `fondos`) usan `getFlowAssessment` para medir adherencia por flujo del período.
- **Frontend**: AllocationChip (teal, junto a FrameworkTabs), AllocationDrawer (lateral 460px), dot en sidebar nav "Salud financiera" (color `--disp`, sin animación, se muestra cuando no hay distribución del mes).
- **Re-distribución del mes**: reemplaza transacciones anteriores del mismo `MonthlyAllocation` sin crear duplicados.
- **AllocationDrawer — inputs con separador de miles**: inputs de ingreso y distribución cambiados de `type="number"` a `type="text" inputMode="numeric"`. Usan `formatThousands` de `money.ts` para display; almacenan solo dígitos. `formatCLP` interno usa `formatThousands` en lugar de `Number().toLocaleString`.
- **GET /monthly-allocation/current — fix null response**: NestJS/Express omitía el body cuando el controller retornaba `null` (llamaba `res.send()` sin cuerpo). Corregido usando `@Res()` y `res.json(allocation)` explícito para enviar JSON `null` literal. Sin este fix React Query recibía body vacío, fallaba el parse y `data` quedaba `undefined` (dot nunca aparecía).
- **AdherenceChart — unificación de semántica**: eliminado el branch `isFondos` que mostraba "saldo como % del total" para fondos propios. Todos los frameworks usan flujo del período. Subtítulo y empty label unificados.
- **AdherenceChart — visualización de flujo negativo**: barras con `actualPercentage < 0` se clampeaban a `width: "-X%"` (CSS inválido, se veía como track lleno). Corregido con `Math.max(0, ...)`. Fondos con flujo negativo muestran barra ámbar + monto ámbar. Marcador y etiqueta "Meta" se ocultan cuando `targetPercentage === 0`.
- **GET /health/assessment — fix porcentajes inflados**: el SQL incluía transferencias entre fondos en el flujo por fondo pero no en el `totalIncome` base. Esto provocaba porcentajes > 100% en fondos que recibían transferencias. Corregido eliminando los dos `UNION ALL` de transferencias — el assessment ahora solo cuenta transacciones (income/expense), coherente con el denominador.
- **Transacciones — fix fondos archivados**: el selector de fondo en `TransactionFormModal` y el dropdown de reasignación en `TransactionsTable` mostraban fondos archivados. Corregido filtrando `fund.archivedAt === null` en ambos lugares.
- **Backend auditado** (2026-06-29): revisión módulo por módulo — calidad de código + unit tests. Resultados:
  - `common/`: `HttpExceptionFilter` global creado → shape de error uniforme `{ statusCode, message, error, timestamp, path }` en toda la API.
  - `auth/`: rechaza cambio a contraseña igual que la actual (`BadRequestException`); 3 tests nuevos (changePassword, email enumeration).
  - `funds/`: 5 tests nuevos (balance derivado, soft-delete, createPreset transaccional).
  - `transactions/`: spec creado desde cero — 9 tests (userId filter, filtros opcionales, paginación, SQL injection).
  - `health/`: spec creado desde cero — 5 tests (assessment percentages, monthlyIncome fallback, framework seeding).
  - `reports/`: sin cambios — SQL parametrizado, guard de división por cero, nombres descriptivos ya en orden.
  - `activity/`: **bug corregido** — `fundId` filter en el UNION excluía todas las transferencias; corregido con `OR from_fund_id = $7 OR to_fund_id = $7`.
  - `import-export/`: 2 tests nuevos (nombre de hoja inválido, celda no numérica).
  - `transfers/`: test de fondo destino no autorizado agregado.
  - `monthly-allocation/`: **bug corregido** — `fundsRepo.find` no filtraba `archivedAt IS NULL`; añadido para evitar distribuciones a fondos archivados. Test correspondiente agregado.

- **Frontend auditado** (2026-06-29): revisión feature por feature — calidad de código, ADR-03, tests para lógica pura. Resultados:
  - **Vitest configurado**: testing infrastructure instalada (`vitest`, `@testing-library/react`, `jsdom`). 48 tests en 7 archivos.
  - **Bugs corregidos**: (1) `minorUnitsToInput` en `TransactionFormModal` usaba `Number(BigInt())` — reemplazado por `BigInt.toString()`. (2) `useCreateTransfer` no invalidaba `['activity']` — actividad quedaba desactualizada tras una transferencia.
  - **ADR-03**: `Number(f.balance)` para aritmética financiera reemplazado por BigInt en `TransfersPage` (aritmética + comparaciones + quickAmounts) y `FundsPage` (totales por clasificación). `Number()` se usa únicamente en puntos de display con comentario.
  - `formatMoney` de `lib/money.ts` reemplaza `new Intl.NumberFormat(...)` inline en `FundsPage`.
  - **MONTH_NAMES deduplicado**: definición única en `dashboard/utils.ts` (exportada como `MONTH_NAMES` + `monthName()`); `Greeting` y `TransactionsPage` actualizados para importar de ahí.
  - `formatChipDate` y `toTableRow` extraídas de `TransactionsPage` a `transactions/utils.ts`.
  - **CommandPalette**: ArrowDown/Up navegan la lista; hover sincroniza el ítem activo; Enter selecciona el activo (no siempre el primero).
  - **Tests nuevos**: `lib/money.spec.ts` (12), `funds/utils.spec.ts` (8), `dashboard/utils.spec.ts` (4), `transactions/utils.spec.ts` (7), `health/utils.spec.ts` (9), `auth/passwordRules.spec.ts` (7).

- **Import — tolerancia de plantillas Excel**: el parser (`excel-ledger.parser.ts`) ya no asume que el nombre de hoja siempre trae año (`"enero 2026"`) ni que el pie de página de totales mide 1 fila. Ahora: (1) detecta el mes por palabra en cualquier parte del nombre de hoja (case-insensitive) en vez de exigir el patrón exacto `mes año`; hojas sin ninguna palabra-mes se ignoran en silencio (antes generaban un `ParseError` falso-positivo); (2) si una hoja trae mes pero no año, el parser corta y devuelve `needsYear: true` — el endpoint `POST /import/preview` acepta un campo `year` opcional (multipart) para ese caso, y el wizard de `/import` pide el año una sola vez antes de continuar (el flujo con plantillas que ya traen año no cambia); (3) el fin de las filas de datos se acota por días reales del mes (`FIRST_DATA_ROW_INDEX + lastDay - 1`) en vez de por la posición asumida del pie de página, tolerando pies de 1, 2 o más filas de resumen sin necesitar detectarlas por contenido. Las columnas de ingreso/gasto no atadas a un fondo (ej. "Sueldo Líquido", "Gastos en el Mes") se siguen ignorando sin cambios — ya no tenían encabezado de fondo en la fila de headers.

- **Import Excel — cleanup del flujo (onboarding + standalone)**: la máquina de estados de "subir Excel → preguntar año → preview" estaba duplicada entre `ImportPage.tsx` (pantalla `/import`) y la rama excel de `Step2Funds.tsx` (paso 2 del onboarding). Se extrajo a un hook único `useExcelImportFlow` (`features/import-export/useExcelImportFlow.ts`), con la lógica pura de validación de año y filtrado de filas a comprometer en `importFlow.utils.ts` (testeada). La rama excel de `Step2Funds.tsx` se separó a un componente nuevo `Step2Excel.tsx`, puramente presentacional — `Step2Funds.tsx` queda solo con la lógica de listar/crear fondos de un preset. El botón "Continuar" del paso de año en onboarding vive ahora en el footer real del wizard (llamando directamente a `flow.submitYear`), reemplazando el `useEffect` de sincronización y el truco de atributo `form=` usados en la implementación original. Se agregó un componente `Button` compartido (`components/ui/Button.tsx`, variantes `primary`/`secondary`, tamaños `sm`/`md`, prop `muted`) usado en `OnboardingPage`, `PreviewStep`, `UploadStep` e `ImportPage` — sin cambios de comportamiento observable, solo estructura interna.

- **Import Excel — fixes post-revisión**: (1) `ImportPreviewDto.year` no tenía cota de rango en el backend (solo `@IsInt()`) — se agregó `@Min(2000)/@Max(2100)`, hallado en la revisión final de rama del cleanup de arriba; test e2e nuevo que confirma 400 para años fuera de rango. (2) El botón "Salir" de `OnboardingPage` cerraba sesión siempre, incluso al llegar por `/onboarding?from=settings` (flujo de reconfigurar fondos desde Ajustes) — ahora, cuando `isReconfigure` es true, el botón dice "Volver a Ajustes" y navega a `/ajustes` en vez de deslogear.

- **Fix: adherencia mostraba 100/100 sin datos (2026-07-01)**: en `/salud`, cuando el usuario no tenía ingresos en el período y tampoco había configurado `monthlyIncome`, `GET /health/assessment` devolvía `totalBase: '0'` con `actualPercentage: 0` en todos los fondos. Para el framework `fondos` (sin `targetPercentage` real por fondo, que cae a `0`), la desviación resultante era 0 y `computeScore` devolvía 100 — un score perfecto engañoso construido sobre ceros. El panel derecho ya mostraba correctamente `HealthEmptyState variant="no-income"` en este caso, pero el `ScoreCard` de la izquierda seguía calculando y mostrando el score. Corregido en `HealthPage.tsx`: se deriva `hasData` (`assessment.funds.length > 0 && totalBase !== '0'`) y el score solo se calcula cuando `hasData` es true; si no, `ScoreCard` recibe `score: null` y renderiza `—/100` en vez de un número. Sin cambios en el backend ni en `computeScore`.

- **Health — auditoría de estandarización (2026-07-01)**: pasada de limpieza sobre `features/health/` siguiendo la nueva regla de `conventions.md` ("Standardization pass on a feature"), análoga al cleanup de onboarding/import. (1) `AllocationDrawer` ahora usa `components/ui/Modal.tsx` en vez de reimplementar backdrop/panel/cierre a mano (gana cierre con Escape). (2) Botones de `AllocationDrawer` y `HealthEmptyState` migrados a `components/ui/Button.tsx`; los de `ScoreCard` se dejaron intencionalmente sin tocar — viven sobre una tarjeta oscura fija con tamaños/hover que no coinciden con la API de `Button`, sin una segunda ocurrencia real a la que converger. (3) `formatCLP` local eliminado, reemplazado por `formatMoney(value, 'CLP')`. (4) Mapeo framework→slot-prefix (duplicado en `AllocationDrawer`, `ScoreCard` y `FrameworkTabs`) centralizado en `frameworkSlotPrefix()`/`isSlotFramework()`/`ALL_FRAMEWORKS` (`health/utils.ts`). (5) Hex hardcodeados (`#16A89A`, `#D97706`, `#DC2626`, y el gradiente literal de `ScoreCard` idéntico a `var(--grad)`) reemplazados por variables de tema (`var(--disp)`, `var(--comp)`, `var(--neg)`, `var(--grad)`) — antes no se adaptaban a modo oscuro. (6) `AllocationDrawer` (~330 líneas) dividido: lógica pura a `allocationDrawer.utils.ts` (testeada en `allocationDrawer.utils.spec.ts`, 10 tests nuevos) y los dos pasos de UI a `AllocationIncomeStep.tsx`/`AllocationDistributionStep.tsx`. (7) `HealthEmptyState` consolidado de dos bloques JSX casi idénticos a un solo render parametrizado por variante. `RecommendationCards` se dejó sin cambios (código muerto intencional, pendiente IA, ya documentado). Sin cambios de comportamiento observable — 67/67 tests de `web` en verde.

- **`app/icons.tsx` movido a `components/ui/icons.tsx` (2026-07-01)**: primer paso de estandarizar los íconos SVG hardcodeados bajo `components/ui/`, junto a `Button`/`Modal`/etc. Solo relocate (sin cambios de comportamiento) — 11 archivos importadores actualizados. Los íconos locales de `features/auth/components/icons.tsx` no se tocaron (son propios de ese feature, no del set compartido). Consolidar el resto del SVG inline disperso por otras features queda pendiente, a hacerse de forma incremental solo cuando se confirme una duplicación real, según la regla de `conventions.md`.

- **Release v1.1.2 (2026-07-02)**: agrupa las tres entradas de arriba (fix de score falso 100/100 en `/salud`, auditoría de estandarización de `features/health/`, relocate de `app/icons.tsx`). PR [#6](https://github.com/ISeco/wealet/pull/6) `dev → main`, aprobado por `ISeco-agsbyte`, CI en verde, mergeado en `1b683e7`. Tag `v1.1.2` sobre el merge commit.

---

## Pendientes documentados

- **Empty states implementados**: `/transferencias` muestra un empty state centrado cuando hay menos de 2 fondos (`funds.length < 2`). `/salud` usa `HealthEmptyState` con variante `no-funds` (assessment sin fondos) y `no-income` (fondos sin flujo del período).

- **Autenticación con Google — completa (backend + frontend)**: flujo end-to-end. Backend: DB schema con `google_id` y `password_hash` nullable, métodos `findByGoogleId`/`createWithGoogle`/`linkGoogleId` en repo, endpoint `POST /auth/google` con google-auth-library. Frontend: `@react-oauth/google` instalado, `GoogleOAuthProvider` en `providers.tsx`, `AuthContext.loginWithGoogle(accessToken)` llama API. Botón "Continuar con Google" en `AuthPage` con flujo de popup. Settings adaptado: `SecurityCard` muestra "Establecer contraseña" para usuarios Google-only, ChangePasswordModal oculta campo "Contraseña actual" cuando `user.hasPassword === false`. `ChangePasswordPayload.currentPassword` ahora opcional. `User.hasPassword: boolean` en el tipo frontend.
- *Recuperación de contraseña implementada (backend + frontend)*: flujo completo. Backend (Tasks 1-5): migración con `passwordResetToken`/`passwordResetExpiresAt` en `User`, endpoints `POST /auth/forgot-password` y `POST /auth/reset-password`, servicio de email con Brevo (plantilla HTML). Frontend (Task 6): `ForgotPasswordPage` (`/forgot-password`) y `ResetPasswordPage` (`/reset-password?token=`) como páginas full-screen sin AppLayout. `LoginForm` incluye link "¿Olvidaste tu contraseña?". `AuthPage` muestra banner de éxito verde cuando `?reset=ok` está presente. `SubmitButton` acepta prop `disabled` opcional. API functions `forgotPassword` y `resetPassword` en `auth/api.ts`. Rutas registradas en el router.
- **Auth con Google — bugs corregidos (2026-06-30)**: el login con Google devolvía 401 siempre. (1) Backend: `auth.service.ts` leía `tokenInfo.user_id` del endpoint tokeninfo de Google, pero ese campo ya no existe en la respuesta actual — el identificador viene en `tokenInfo.sub`. Corregido usando `sub`. (2) Frontend: tras un 200 exitoso, `AuthPage` seguía mostrando `/login` — el handler `onSuccess` de `useGoogleLogin` actualizaba el estado de auth pero nunca llamaba `navigate('/')`, a diferencia de `LoginForm`/`RegisterForm`. Corregido agregando `navigate('/', { replace: true })` tras `loginWithGoogle`.
- **Vercel — sesión se perdía al refrescar (2026-06-30)**: dos causas distintas, sin cambios de código necesarios para la primera.
  - (1) La API en Render tenía `NODE_ENV=development`, así que `auth.controller.ts` emitía la cookie `refresh_token` sin `secure`/`sameSite: 'none'`. Como el frontend (`wealet-web.vercel.app`) y la API (Render) son dominios distintos, el navegador descartaba la cookie en cada request cross-site, y el refresh tras recargar la página devolvía 401. Fix: setear `NODE_ENV=production` en las env vars del servicio de Render (no es un bug de código).
  - (2) Vercel devolvía 404 al refrescar en rutas no-raíz (ej. `/login`) porque no había `vercel.json` con rewrite para SPA routing — el GET directo a esa ruta no encontraba un archivo físico. Agregado `apps/web/vercel.json` con rewrite `/(.*) -> /index.html`. Redeploy confirmado en producción (2026-07-02).
- **Fix: fechas futuras en transacciones/transferencias (2026-06-30)**: `DateInput` permitía seleccionar cualquier fecha sin tope. Frontend: se agregó prop `maxDate` (ISO) al componente — deshabilita días/meses futuros en el calendario y los marca visualmente; usado con la fecha de hoy en `TransactionFormModal`, `TransfersPage` y los filtros de rango de `TransactionsToolbar`. Backend: nuevo validador `@IsNotFutureDate()` en `common/validators/`, aplicado a `occurredOn` en `CreateTransactionDto`, `UpdateTransactionDto` y `CreateTransferDto` para cerrar el boundary ante llamadas directas a la API. No se tocó `ImportRowDto` (importación de datos históricos vía Excel).
- **Fix: CommandPalette se desbordaba de la pantalla (2026-06-30)**: con muchos fondos, la lista de resultados (Navegar a / Crear / Fondos recientes / Fondos) no tenía límite de alto ni scroll, y se cortaba contra el borde de la ventana. Estado vacío (sin búsqueda) ahora muestra solo "Acciones rápidas" (4 items: nueva transacción/transferencia/fondo/categoría, con ícono de color) más un mensaje "Escribe para buscar fondos, pantallas y otras opciones de la app.", fiel a `docs/design/screens/palette-quick.html`. Al escribir, se mantiene la búsqueda completa (nav + acciones + fondos) pero el contenedor de resultados ahora tiene `max-height` con scroll interno en vez de desbordar.
- **Migración de Postgres: Render → Supabase (2026-07-01)**: Render borra la DB a los 90 días en su tier gratuito. (1) `database.config.ts`/`typeorm-datasource.ts` activaban SSL solo si `DATABASE_URL` contenía `'render.com'` — hardcoded al proveedor, rompía con cualquier otro host. Extraído a `isRemoteDatabase()` (`config/is-remote-database-url.ts`), que activa SSL para cualquier host que no sea `localhost`/`127.0.0.1`. (2) Bug de seguimiento: el chequeo solo miraba `DATABASE_URL` — al conectar con las variables sueltas (`DB_HOST`/`DB_USERNAME`/etc., sin `DATABASE_URL`), SSL quedaba desactivado y Supabase respondía `ESSLREQUIRED`. Corregido para que `isRemoteDatabase()` también reciba `DB_HOST` y lo use cuando `DATABASE_URL` no está seteado. Verificado en Render conectando a Supabase.

- **Fix: `/funds` listaba fondos de frameworks anteriores mezclados (2026-07-02)**: `seedFrameworkFunds` (health.service.ts) solo agrega los fondos-slot que faltan al cambiar de framework — nunca archiva los del framework previamente activo, así que quedaban activos para siempre. Primer intento (descartado): filtrar `GET /funds` con `?activeFrameworkOnly=true` para que `/funds` solo mostrara los del framework activo — se revirtió porque ocultaba por completo los fondos propios del usuario (sin `frameworkSlot`) cuando había un framework de slots activo, dando la falsa sensación de que el fondo recién creado no se había guardado (un framework no debe limitar la libertad de crear fondos propios). Fix final: `/funds` sigue trayendo todos los fondos vía `useFunds()` (sin cambios en el backend) y agrega tabs de scope **Todas / Del framework / Propios** en `FundsPage.tsx`, igual al patrón ya usado en Categorías (Todas/Mías/Sistema) — filtrado 100% client-side por `frameworkSlot !== null` ("Del framework") vs `=== null` ("Propios"), sin depender del framework activo (el label "Del framework" se mantuvo por ser más claro para el usuario que "Con slot", aunque internamente el filtro es `frameworkSlot !== null`, no un match contra `HealthProfile.framework`). Primera versión de las tabs sí matcheaba el framework activo vía `fundMatchesFramework`, pero se descartó tras revisión: cuando el framework activo es `fondos`, esa versión de "Del framework" y "Propios" eran el mismo conjunto por definición, y los fondos-slot huérfanos de un framework anterior quedaban indistinguibles solo en "Todas" sin ninguna tab que los agrupara — confuso. La versión actual siempre distingue algo si hay al menos un fondo-slot (activo u huérfano), igual que Categorías no distingue "sistema activo" de "sistema" a secas. De paso, `RunwayCard.tsx` (Ajustes) tenía su propio `matchesFramework` local y desactualizado (le faltaba el caso `profit_first`, esos fondos nunca aparecían en el toggle de runway) — reemplazado por el helper compartido `fundMatchesFramework` de `health/utils.ts` (ese helper sigue vivo solo para este uso, ya no lo usa `/funds`); este fix se mantiene. Pendiente (fuera de alcance, documentado pero no resuelto): archivar automáticamente los fondos-slot del framework anterior al cambiar de framework en `seedFrameworkFunds`, para que no queden huérfanos indefinidamente (sí seguirán viéndose en las tabs "Todas" y "Del framework" de `/funds`).

- **Fix: puerto local de Postgres en conflicto (2026-07-02)**: `docker-compose.yml` mapeaba `5432:5432`, pero un servicio nativo de Windows (`postgresql-x64-18`) ya estaba enlazado a `0.0.0.0:5432`, interceptando silenciosamente las conexiones destinadas al contenedor (que solo tenía el lado IPv6 del mismo puerto). La API autenticaba contra la instancia equivocada de Postgres y fallaba con "password authentication failed". Corregido remapeando el contenedor a `5433:5432` (`docker-compose.yml`, `apps/api/.env`, `apps/api/.env.test`) sin tocar el servicio nativo.

- **Release v1.1.3 (2026-07-02)**: agrupa las tres entradas de arriba (tabs de scope en `/funds`, fix de `RunwayCard.tsx` para `profit_first`, fix de conflicto de puerto local de Postgres) más el TODO de standardization pass documentado para Funds/Transactions/Transfers/Categories/Settings/Health/Auth/Dashboard. PR [#7](https://github.com/ISeco/wealet/pull/7) `dev → main`, aprobado por `ISeco-agsbyte`, CI en verde, mergeado en `4aceed1`. Tag `v1.1.3` sobre el merge commit.

- **TODO: Standardization pass on a feature — web (pendiente)**: aplicar la revisión de `conventions.md` ("Standardization pass on a feature") a las features de `apps/web` que todavía no la tuvieron: Dashboard (Categories quedó parcialmente cubierta al migrar su toggle "Tipo" y sus scope tabs a `SegmentedTabs` durante la pasada de Transacciones, pero no tuvo una auditoría completa propia todavía). Ya se hizo en Health (2026-07-01), Funds (2026-07-02), Transactions (2026-07-02), Transfers (2026-07-02), Settings (2026-07-02) y Auth (2026-07-02, ver entradas arriba), y parcialmente en Onboarding/Import (cleanup) — sirven de referencia del alcance esperado. Revisar por feature: UI hand-rolled que duplica `components/ui/` (Modal, Button, icons, SegmentedTabs), formato de dinero fuera de `lib/money.ts`, hex hardcodeados que no siguen `var(--...)` de `index.css`, el mismo dato de dominio codificado en más de un lugar, y god components según el criterio de extracción de sección (40+ líneas / estado independiente). Fix solo ante una segunda ocurrencia confirmada — no refactor especulativo.

- **Funds — auditoría de estandarización (2026-07-02)**: pasada de `features/funds/` siguiendo la regla de `conventions.md`. (1) `classColor()` (`utils.ts`) traía un campo `cssColor` con hex hardcodeados (`#16A89A`/`#2563EB`/`#D97706`) que nadie consumía — el color real ya lo daba el campo `color` (`var(--disp)`/etc.); eliminado. (2) `FundFormDrawer` mantenía su propio array `CLASSIFICATIONS` (label + color por clasificación) duplicando exactamente lo que ya devuelve `classColor()` — reemplazado por `CLASS_ORDER.map(classColor)`; `CLASS_ORDER` se movió de `FundsPage.tsx` a `utils.ts` como fuente única. (3) Botones hand-rolled migrados a `components/ui/Button.tsx` (ya establecido en Health/Onboarding/Import): el CTA "Nuevo fondo" y el de `EmptyState` en `FundsPage`, y el footer Cancelar/Guardar de `FundFormDrawer`. El botón icon-only de eliminar (42×42, `TrashIcon`) se dejó igual — coincide con el mismo patrón ya usado en `TransactionFormModal`, no diverge. (4) `FundDetail.tsx` tenía su propio array `MONTH_NAMES_ES` (minúsculas) — reemplazado por el `MONTH_NAMES` compartido de `dashboard/utils.ts` (`.toLowerCase()` en el punto de uso). `PRESET_COLORS` de `FundFormDrawer` (paleta de color libre para el fondo) se dejó igual — no es lo mismo que `classColor()`, son colores que el usuario elige libremente, no la clasificación. Sin cambios de comportamiento observable — 67/67 tests de `web` en verde, `type-check` y `lint` limpios.

- **Funds — botón de eliminar movido al detalle del fondo (2026-07-02)**: a pedido del usuario, el botón de eliminar (antes dentro del footer de `FundFormDrawer`, junto a Cancelar/Guardar) se movió al header de `FundDetail.tsx`, junto a "Editar" — más accesible sin necesidad de abrir el drawer de edición primero. `FundFormDrawer` quedó solo con create/edit (perdió `onDelete` prop, `useDeleteFund`, `ConfirmDialog` y el botón icon-only); `FundDetail.tsx` ahora tiene su propio `useDeleteFund` + `ConfirmDialog` + estado de error, igual al patrón ya usado en `CategoriesPage`. Primera versión de este cambio reintrodujo botones `<button>` hand-rolled para "Eliminar"/"Editar" en vez de usar `components/ui/Button.tsx` — pasó desapercibido porque el criterio de "sin segunda ocurrencia, no tocar" de la auditoría anterior aplicaba a código *preexistente* (Volver/Editar ya estaban así), no a código nuevo escrito en la misma sesión. Corregido: los tres botones del header (Volver/Eliminar/Editar) ahora usan `Button` con `variant="secondary"` y overrides de `style` para el alto 32px propio de ese header. Se agregó una regla a `CLAUDE.md` ("Standardization applies to new code too") para que esto no vuelva a pasar. 67/67 tests en verde, `type-check` y `lint` limpios.

- **Fix: fondos-slot huérfanos ahora se archivan automáticamente al cambiar de framework (2026-07-02)**: resuelve el pendiente dejado abierto en la entrada anterior ("archivar automáticamente los fondos-slot del framework anterior... para que no queden huérfanos indefinidamente"). `HealthService.provisionFrameworkFunds` (antes `seedFrameworkFunds`, ahora pública y transaccional) archiva los fondos-slot del framework saliente y reactiva — misma fila, mismo historial — los del framework entrante si ya existían archivados de un uso anterior; solo crea fondos nuevos para slots sin fila previa. `POST /funds/preset` (usado por el wizard de onboarding, incluido "Reconfigurar fondos" desde Ajustes) dejó de construir fondos desde su propia copia de templates (`FUND_PRESETS`, eliminado) y ahora delega en el mismo método — antes eran dos caminos distintos y solo el de Ajustes (`PATCH /health/profile`) archivaba correctamente; onboarding creaba fondos duplicados vacíos en vez de reactivar los históricos. Migración `ScopeFundNameUniqueToActiveFunds`: el índice único de nombre de fondo pasó de aplicar solo a fondos propios (`framework_slot IS NULL`) a aplicar a *todos* los fondos activos (`archived_at IS NULL`) — un fondo propio y uno de framework ya no pueden compartir nombre mientras ambos estén visibles, pero archivar libera el nombre para reutilizarlo (necesario para que la reactivación funcione). La migración incluye un data-fix que archiva duplicados activos preexistentes (el bug original) antes de crear el índice. `GET /reports/runway` ahora excluye fondos archivados (antes los seguía sumando aunque `countsForRunway` ya no fuera visible/editable para el usuario). Frontend: `PreferencesCard` (Ajustes) y el paso 1 del wizard de onboarding en modo reconfigurar muestran un `ConfirmDialog` explicando qué se archivará antes de aplicar el cambio de framework.

- **Fix: 3 hallazgos de la revisión final de rama en `HealthService.provisionFrameworkFunds` (2026-07-02)**: (1) Crítico — el índice único parcial `UQ_funds_user_name_active` se valida por statement, no diferido; `fundsRepo.save([...toArchive, ...result])` ejecutaba todos los INSERT antes que los UPDATE (comportamiento de TypeORM en arrays mixtos), así que al cambiar entre frameworks que comparten nombre de slot (ej. "Necesidades" en `50_30_20` y `jars_eker`) el INSERT del fondo entrante corría antes del UPDATE que archivaba el saliente, violando el índice y abortando la transacción con un 500. Corregido separando en dos `save()` secuenciales dentro de la misma transacción: primero `fundsRepo.save(toArchive)`, después `fundsRepo.save(result)`. (2) Importante — sin traducción de `QueryFailedError`/`23505` a `ConflictException`, una colisión de nombre legítima (fondo propio activo con el mismo nombre que un slot del framework entrante) daba 500 en vez de 409; agregado el mismo patrón try/catch ya usado en `funds.service.ts`. (3) Importante — al reactivar un fondo archivado, `isOperative` quedaba en `false` para siempre (lo fuerza el paso de archivado) aunque el template del slot dijera `isOperative: true`; ahora se restaura desde el template en la rama de reactivación (`existing.isOperative = template.isOperative`), preservando `name`/`classification`/`targetPercentage` tal como estaban (customización real del usuario) y dejando `countsForRunway` intacto (no se toca en archivado, tampoco en reactivación). Tests: `health.service.spec.ts` (orden de los dos `save()`, traducción a `ConflictException`, restauración de `isOperative` en reactivación) y nuevo e2e `apps/api/test/framework-switch.e2e-spec.ts` (switch real `50_30_20 → jars_eker → 50_30_20` contra Postgres, verificando que `GET /funds` nunca muestra dos fondos activos con el mismo nombre).

- **Fix: aviso de archivado incorrecto al salir de Fondos Propios (2026-07-02)**: el `ConfirmDialog` de cambio de framework (en `PreferencesCard.tsx` de Ajustes y en `OnboardingPage.tsx` modo reconfigurar) siempre advertía "los fondos de [framework actual] se archivarán", pero Fondos Propios no tiene fondos-slot — salir de ese framework nunca archiva nada. Corregido con un mensaje condicional: al salir de `fondos` explica que los fondos propios no se ven afectados y que se activarán los del framework destino; en cualquier otro caso mantiene el mensaje de archivado/reactivación ya existente.

- **Release v1.2.0 (2026-07-02)**: agrupa el archivado automático de fondos-slot huérfanos al cambiar de framework, los 3 hallazgos de revisión de `provisionFrameworkFunds`, y el fix del aviso de archivado al salir de Fondos Propios (las tres entradas de arriba). PR [#9](https://github.com/ISeco/wealet/pull/9) `dev → main`, aprobado y mergeado por `ISeco-agsbyte`, CI en verde, mergeado en `144ced3`. Tag `v1.2.0` sobre el merge commit. `vercel.json` (rewrite SPA de la entrada del 2026-06-30) ya está en producción — pendiente #3 resuelto.

- **Release v1.3.0 (2026-07-02)**: agrupa la auditoría de estandarización de Funds y el movimiento del botón de eliminar al detalle del fondo (las dos entradas de arriba). PR [#10](https://github.com/ISeco/wealet/pull/10) `dev → main`, aprobado y mergeado por `ISeco-agsbyte`, CI y Vercel en verde, mergeado en `f617538`. Tag `v1.3.0` sobre el merge commit. Redeploy en Vercel confirmado en producción (2026-07-02).

- **Transacciones — extracción de lógica de `TransactionsPage.tsx` (2026-07-02)**: revisión de organización de lógica sobre `features/transactions/` (más allá del checklist de duplicación de UI de la auditoría de arriba). Tres hallazgos corregidos: (1) la orquestación de URL/filtros (lectura de `tab`/`search`/`page`/`filters` desde `searchParams` + los cuatro handlers casi idénticos que clonaban `URLSearchParams`, mutaban y borraban `page`) vivía entera dentro del componente de página — extraída a `useTransactionsUrlState.ts`, mismo patrón que `useExcelImportFlow` (cleanup de Import); `TransactionsPage.tsx` queda como composición, delegando la lectura/escritura de cada filtro al hook. (2) El mapeo tab→`type`/`subtype` para `/activity`, antes inline en un `useMemo` del componente, se movió a `buildActivityQuery()` (función pura, testeada) en `transactions/utils.ts`, junto a `formatChipDate`/`toTableRow`. (3) El filtro "fondos activos" (`!f.archivedAt`) estaba triplicado — `TransactionsPage.tsx`, `TransactionsTable.tsx` (redundante: filtraba de nuevo sobre `funds`, que la página ya pasaba filtrado) y el `Select` de fondo en `TransactionFormModal.tsx` — centralizado en `activeFunds()` (`features/funds/utils.ts`, exportado desde `features/funds/index.ts`) y usado en los tres sitios; se eliminó el filtro redundante en la tabla. Tests nuevos para `buildActivityQuery` y `activeFunds`. 74/74 tests de `web`, `type-check`, `lint` y build de producción en verde.

- **Transacciones — auditoría de estandarización (2026-07-02)**: pasada de `features/transactions/` siguiendo `conventions.md`. Al auditar `TransactionsTabs` se detectó que el mismo control de "tabs segmentadas" (contenedor `var(--card-2)` + tab activo con `var(--card)`/`var(--shadow)`) estaba reimplementado a mano en 6 lugares además de Transacciones: `FundsPage` (scope tabs), `CategoriesPage` (scope tabs), `FrameworkTabs` (health), el toggle "Tipo" de `TransactionFormModal` y de `CategoryFormDrawer`. Se extrajo `components/ui/SegmentedTabs.tsx` (value/onChange/options genérico, soporta `fullWidth` para toggles de 2 opciones y `activeColor` por opción para casos como Gasto=neg/Ingreso=pos) y se migraron los 6 sitios. **Excepción deliberada**: el toggle Login/Registro de `AuthPage` usa el mismo estilo visual pero con `<Link>` de react-router (navegación real de página, no un `onChange` de estado) — se dejó sin tocar porque la semántica es distinta, no una divergencia silenciosa. Además, dentro de Transacciones: botones hand-rolled (`Cancelar`/`Guardar`/eliminar en `TransactionFormModal`; `Filtros`/`Exportar`/`Nueva`/`Limpiar`/`Aplicar` en `TransactionsToolbar`) migrados a `components/ui/Button.tsx` con overrides de `style` para alturas no estándar (38px/42px), mismo patrón ya usado en `FundDetail.tsx`. Íconos SVG inline que duplicaban `components/ui/icons.tsx` (la "×" de los chips de filtro y del popover, el chevron del selector de reasignación de fondo) reemplazados por `CloseIcon`/`ChevronDownIcon`. `TransactionsTable` y `TransactionFormModal` no calificaron para god-component (secciones no tienen estado independiente entre sí). Sin cambios de comportamiento observable — 67/67 tests de `web`, `type-check`, `lint` y `build` de producción en verde.

- **Release v1.4.0 (2026-07-02)**: agrupa la auditoría de estandarización de Transacciones (`SegmentedTabs` compartido, migración de botones/íconos a `components/ui/`) y la extracción de lógica de `TransactionsPage.tsx` (`useTransactionsUrlState`, `buildActivityQuery`, `activeFunds` centralizado — las dos entradas de arriba). PR [#11](https://github.com/ISeco/wealet/pull/11) `dev → main`, aprobado y mergeado por `ISeco-agsbyte`, mergeado en `c433886`. Tag `v1.4.0` sobre el merge commit.

- **Transfers — auditoría de estandarización (2026-07-02)**: pasada de `features/transfers/` siguiendo `conventions.md`, primera de las cuatro pendientes documentadas arriba (Transfers, Settings, Auth, Dashboard). (1) `TransfersPage.tsx` y `FundPicker.tsx` tenían cada uno su propio `Intl.NumberFormat('es-CL', { style: 'currency', currency: 'CLP' })` local — reemplazados por `formatMoney(value.toString(), 'CLP')` de `lib/money.ts`; el input de monto usaba `parseInt(rawAmount, 10).toLocaleString('es-CL')` para el separador de miles — reemplazado por `formatThousands(rawAmount)`, mismo patrón ya usado en `AllocationDrawer`/`TransactionFormModal`. (2) Botones hand-rolled (Ir a Fondos, Hacer otra, Ver fondo, Cancelar, Transferir) migrados a `components/ui/Button.tsx` con overrides de `style` para altos no estándar, mismo patrón que `FundDetail.tsx`/Transacciones. (3) Dos íconos SVG inline se repetían 3-4 veces dentro del propio archivo y también en otras features ya auditadas — se agregaron `CheckIcon` y `ArrowRightIcon` a `components/ui/icons.tsx` (confirmado grep: check en `SuccessStep.tsx`/`auth/components/icons.tsx`, flecha en `Step1Preset.tsx` de onboarding) y se migraron los usos dentro de Transfers; los usos en otras features quedan para cuando se audite esa feature, no se tocó código fuera de alcance. El chevron y el ícono "+" hand-rolled de `FundPicker` (placeholder de fondo sin seleccionar) se reemplazaron por `ChevronDownIcon`/`PlusIcon`, ya existentes. Sin cambios de comportamiento observable — 74/74 tests de `web`, `type-check` y `lint` limpios. (2) Lógica pura extraída a `transfers/utils.ts` (feature no tenía `utils.ts` propio, a diferencia de Health/Funds/Transactions): `formatDateLabel` (antes inline en la página), `computeQuickAmounts` (splits 25/50/75/Todo del saldo) y `validateTransferAmount` (las dos validaciones de monto de `handleSubmit`, antes mezcladas con las llamadas a `setAmountError`). 10 tests nuevos en `utils.spec.ts` — 84/84 tests de `web` en verde.

- **Settings — auditoría de estandarización (2026-07-02)**: pasada de `features/settings/` siguiendo `conventions.md`, segunda de las pendientes (Transfers, Settings, Auth, Dashboard). (1) Botones hand-rolled en `ProfileCard` (Guardar/Cancelar), `SecurityCard` (Cambiar/Establecer contraseña, Próximamente deshabilitado), `ChangePasswordModal` (footer Cancelar/Guardar) y `DataAccountGrid` (Importar, Exportar, Reconfigurar fondos, Cerrar sesión) migrados a `components/ui/Button.tsx`, mismo patrón de overrides de `style` ya usado en Funds/Transacciones/Transfers. (2) El toggle Claro/Oscuro de `AppearanceCard` reimplementaba a mano el mismo control de "tabs segmentadas" que ya se centralizó en `components/ui/SegmentedTabs.tsx` durante la auditoría de Transacciones — reemplazado (sin `fullWidth`, 2 opciones). (3) Bug menor corregido de paso: el botón de exportar decía "Exportar todo (.csv)" pero `exportAll()` (`settings/api.ts`) siempre descarga un `.xlsx` (`wealet-export.xlsx`) — corregido el label. El toggle de runway (`RunwayCard`, switch on/off) y los íconos `UploadIcon`/`DownloadIcon` de `DataAccountGrid` no calificaron para migrar — sin segunda ocurrencia confirmada en el resto de la app (no hay otro switch ni otros íconos de upload/download compartidos). `SettingsPage.tsx` ya era composición pura (6 cards) y `hooks.ts` ya delegaba correctamente a `api.ts`. (4) Revisión de lógica más a fondo (a pedido explícito de revisar esto): `PreferencesCard.tsx` tenía su propio `FRAMEWORKS`/`FRAMEWORK_LABEL` (label por framework) duplicando `FRAMEWORK_LABELS`/`ALL_FRAMEWORKS` ya existentes en `health/utils.ts` — con drift real ya presente ("Jars of Eker" local vs "Jars de Eker" en `health/utils.ts", "Fondos" vs "Fondos con propósito"); reemplazado por los exports compartidos. (5) El cálculo de iniciales del usuario (`user.displayName`/`email` → 2 letras mayúsculas) estaba duplicado byte a byte entre `ProfileCard.tsx` y `app/Sidebar.tsx` — extraído a `getUserInitials()` (`features/auth/utils.ts`, exportado desde `features/auth/index.ts`) y usado en ambos sitios. Las mismas etiquetas de framework siguen duplicadas en `onboarding/` (`OnboardingPage.tsx`, `Step2Funds.tsx`, `Step3Success.tsx`) — fuera de alcance de esta pasada, queda para cuando se audite Onboarding. Sin cambios de comportamiento observable — 84/84 tests de `web`, `type-check` y `lint` limpios.

- **Auth — auditoría de estandarización (2026-07-02)**: pasada de `features/auth/` siguiendo `conventions.md`, tercera de las pendientes (Transfers, Settings, Auth, Dashboard). (1) El botón "Continuar con Google" de `AuthPage.tsx` y el componente compartido `SubmitButton.tsx` (usado por `LoginForm`, `RegisterForm`, `ForgotPasswordPage`, `ResetPasswordPage`) eran botones hand-rolled — migrados a `components/ui/Button.tsx` (`variant="secondary"` y `variant="primary"` respectivamente) con overrides de `style` para alto/tamaño no estándar, mismo patrón ya usado en Funds/Transacciones/Transfers/Settings. (2) `#12294A` hardcodeado en el panel de branding de `AuthPage.tsx` reemplazado por `var(--navy)` (match exacto en `index.css`, antes no se adaptaba a modo oscuro). (3) `RuleStatusIcon` (`features/auth/components/icons.tsx`) reimplementaba a mano el mismo path de check (`M20 6L9 17l-5-5`) que `components/ui/icons.tsx`'s `CheckIcon` — señalado como pendiente en la auditoría de Transfers (donde se creó `CheckIcon` notando esta ocurrencia en auth sin migrar); ahora consolidado. El toggle Login/Registro de `AuthPage.tsx` se confirmó que sigue usando `<Link>` de react-router (navegación real, no `onChange` de estado) — excepción ya documentada en la auditoría de Transacciones, sigue vigente, sin cambios. No se encontró formato de dinero local, markup de modal, ni duplicación de datos de dominio (reglas de contraseña, cálculo de iniciales ya centralizado) en esta feature. Sin cambios de comportamiento observable — 84/84 tests de `web`, `type-check`, `lint` y build de producción en verde.

- **Auth — limpieza de lógica adicional (2026-07-02)**: dos hallazgos menores de organización de lógica dentro de `features/auth/`, sin segunda ocurrencia cross-file confirmada en el momento de la auditoría anterior pero aplicados a pedido explícito. (1) `EMAIL_REGEX` estaba definido en `RegisterForm.tsx` y usado dos veces en el mismo archivo (`handleSubmit` y `onBlur` de `EmailField`) sin extraerse — movido a `isValidEmail()` en `features/auth/utils.ts`, junto a `getUserInitials`. (2) El wrapper full-screen (`minHeight:'100vh', display:'flex', alignItems:'center', justifyContent:'center', background:'var(--bg)', padding:'40px 24px'` + contenedor `maxWidth:392`) estaba duplicado 4 veces: 3 en `ResetPasswordPage.tsx` (estado sin token / token inválido / formulario principal) y 1 en `ForgotPasswordPage.tsx` — confirmado como ocurrencia cross-file real, extraído a `AuthCenteredLayout` (`features/auth/components/AuthCenteredLayout.tsx`) y usado en ambas páginas. Sin cambios de comportamiento observable — 84/84 tests de `web`, `type-check`, `lint` y build de producción en verde.

- **Dashboard — auditoría de estandarización (2026-07-02)**: pasada de `features/dashboard/` siguiendo `conventions.md`, cuarta y última de las pendientes documentadas (Transfers, Settings, Auth, Dashboard). `DashboardPage.tsx` ya era composición pura (162 líneas, cada sección en su propio archivo) — sin god components. (1) Los botones hand-rolled del empty-state en `DashboardPage.tsx` migrados a `components/ui/Button.tsx`, mismo patrón de overrides de `style` ya usado en el resto de features auditadas. (2) `MonthSelector.tsx` reimplementaba a mano 4 íconos (chevron izq/der, chevron abajo, check) ya existentes en `components/ui/icons.tsx` (`ChevronLeftIcon`/`ChevronRightIcon`/`ChevronDownIcon`/`CheckIcon`, paths idénticos) — reemplazados por los compartidos. (3) `HealthCard.tsx` tenía `STATUS_TEAL`/`STATUS_AMBER` hardcodeados (`#16A89A`/`#D97706`) mientras el `CLS_COLOR` del mismo archivo, tres líneas arriba, ya usaba `var(--disp)`/`var(--comp)` para los mismos colores — duplicado directo en el mismo archivo, corregido. (4) `CategoryChart.tsx`/`CategoryChartDrawer.tsx` tenían cada uno su propio array `PALETTE` con hex hardcodeados (`#16A89A`,`#2563EB`,`#D97706`,`#DC2626`) que no se adaptaban a modo oscuro — reemplazados por `var(--disp)`,`var(--info)`,`var(--warn)`,`var(--neg)` (los dos colores categóricos restantes de la paleta, `#7C3AED`/`#0891B2`, no matchean ningún token y se dejaron igual). (5) Mismo dato de dominio en más de un lugar: `CategoryChart.tsx`/`CategoryChartDrawer.tsx` redeclaraban un `MONTH_NAMES` local idéntico al `MONTH_NAMES`/`monthName()` ya exportado de `dashboard/utils.ts` (el mismo array que Funds ya migró a importar) — reemplazado por `monthName(month)`. `RunwayFundsDrawer.tsx` (`CLASS_STYLES`) y `HealthCard.tsx` (`CLS_COLOR`, solo colores) duplicaban `classColor()` de `funds/utils.ts` — reemplazados por el helper compartido. `RunwayCard.tsx`/`RunwayFundsDrawer.tsx` filtraban fondos con `f.archivedAt === null` en vez de `activeFunds()` de `funds/utils.ts` — reemplazado. Formato de dinero ya usaba `formatMoney` en las 7 tarjetas/drawers, sin hallazgos. (6) Lógica pura duplicada: `CategoryChart.tsx`/`CategoryChartDrawer.tsx` repetían byte a byte el mismo sort de categorías por monto (`BigInt` desc) — extraído a `sortCategoriesByAmountDesc()` (`dashboard/utils.ts`, 3 tests nuevos). El resto de la lógica por componente (`segmentWidths` en `PatrimonioCard`, cálculo de balance/badge en `StatRow`, `sparkHeights`/`cushionTotal` en Runway, top-3 de `HealthCard`) no tiene una segunda ocurrencia — se dejó inline. Botones chromeless tipo link ("Ver todas"/"Ver fondos colchón"/"Ver todos") y el nav icon-only de `MonthSelector` se dejaron sin migrar a `Button` — no hay variante de la API que los cubra, mismo criterio ya aplicado en Health/Funds. `RunwayCard.tsx` (hero card siempre oscuro, `#0F2240` + gradiente) se dejó igual, mismo caso que `ScoreCard` de Salud (auditoría de Health). Sin cambios de comportamiento observable — 84/84 tests de `web`, `type-check` y `lint` limpios.

- **Onboarding + Import-export — auditoría de estandarización (2026-07-02)**: pasada de `features/onboarding/` e `features/import-export/`, cerrando el pendiente de "cleanup parcial" mencionado en las auditorías anteriores. Onboarding: (1) la etiqueta de framework estaba triplicada — `FRAMEWORK_LABEL` en `OnboardingPage.tsx` ("Jars of Eker"), `PRESET_NAMES` en `Step2Funds.tsx` ("Regla 50/30/20") y otro `PRESET_NAMES` en `Step3Success.tsx` ("Fondos propios"), las tres con texto distinto entre sí y respecto a `FRAMEWORK_LABELS` de `health/utils.ts` — las tres reemplazadas por el export compartido (`Step3Success.tsx` lo extiende localmente solo para la clave `excel`, que no es un `HealthFramework`). (2) Botones/select/íconos hand-rolled migrados a `components/ui/`: `AddFundForm.tsx` (botones Agregar/Cancelar → `Button`, `<select>` → `Select`), `FundRow.tsx` (ícono de eliminar → `CloseIcon`), `Step1Preset.tsx` (check y flecha → `CheckIcon`/`ArrowRightIcon`), `Step3Success.tsx` (check → `CheckIcon`, botón primario → `Button`), `Step2Funds.tsx` (botón punteado "Agregar fondo" → `Button` con override de estilo). (3) `Step3Income.tsx` tenía su propio `Intl.NumberFormat('es-CL')` para el separador de miles del input de ingreso — reemplazado por `formatThousands()` de `lib/money.ts`. Import-export: (4) `PreviewStep.tsx` tenía un paginador prev/next hand-rolled (con un comentario `{/* Pagination */}` encima) pese a que `components/ui/Pagination.tsx` ya es el patrón establecido (usado en `TransactionsPage.tsx`) — reemplazado. (5) `SuccessStep.tsx` tenía dos botones y un ícono de check hand-rolled, inconsistente con `ImportPage.tsx` de la misma feature que ya usa `Button` — migrados a `Button`/`CheckIcon`. Sin cambios de comportamiento observable — `type-check` y `lint` de `web` limpios.

- **Onboarding + Import-export — extracción de lógica y fix de UI duplicada (2026-07-02)**: segunda pasada sobre las mismas dos features, esta vez de organización de lógica (no UI), a pedido explícito tras revisar la anterior. (1) `OnboardingPage.tsx` concentraba todo el step-machine del wizard inline (8 `useState`, 6 handlers async/sync, cálculos derivados de `isSlotPreset`/`totalSteps`/`dotStep`/`dots`/`targetFramework`/`willChangeFramework`) mezclado con ~150 líneas de JSX — extraído a `hooks/useOnboardingFlow.ts` (mismo patrón que `useTransactionsUrlState` en Transacciones y `transfers/utils.ts` en Transfers); `OnboardingPage.tsx` queda como composición pura que consume el hook. (2) `PreviewStep.tsx` (import-export) tenía `rowBadge()`/`rowOpacity()` — dos funciones puras de clasificación de fila — definidas fuera de cualquier `utils.ts`, pese a que la feature ya tiene `importFlow.utils.ts` para la lógica del flujo Excel; movidas ahí junto con 7 tests nuevos. (3) Bug de UI encontrado en producción por el usuario: durante el flujo de importar Excel *dentro del onboarding* (`Step2Excel` → `PreviewStep`), se veían dos botones de "volver" a la vez — el footer propio de `PreviewStep` ("← Volver", que reinicia el sub-flujo de Excel a la fase de subida) y el "Atrás" genérico de `OnboardingPage.tsx` (que abandona Excel por completo y vuelve a la selección de preset), porque el nav-footer global se renderizaba sin excluir esa combinación. Corregido: `showNavFooter` ahora también verifica `!(selected === 'excel' && excelFlow.phase === 'preview')`, dejando que `PreviewStep` sea la única fuente de navegación en esa fase (el flujo standalone `ImportPage.tsx`, fuera de onboarding, no tenía este problema porque no superpone un nav-footer propio). Sin cambios de comportamiento observable fuera del fix — 94/94 tests de `web` (+7 nuevos), `type-check` y `lint` limpios.
