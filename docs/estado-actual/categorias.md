# Categorías

- CRUD + color. Tabs de scope Todas/Mías/Sistema (mismo patrón que Fondos), migradas a `SegmentedTabs` compartido.
- Responsive: header (texto + botón "Nueva categoría") apila bajo 600px de ancho de contenedor; el botón tiene `min-height: 44px` en mobile (touch target).
- Sin auditoría de estandarización completa propia todavía — solo la migración de tabs/toggle a `SegmentedTabs`. Ver `pendientes.md`.
